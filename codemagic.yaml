workflows:
  android-workflow:
    name: Build Android APK
    instance_type: linux
    scripts:
      - name: Build APK
        script: |
          cd android_app
          ./gradlew assembleRelease
    artifacts:
      - android_app/app/build/outputs/**/*.apk
